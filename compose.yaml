services:
  back:
    build: backend/
    container_name: backend
    ports:
      - 3001:3001
    env_file:
      - .env
    networks:
      app:
  front:
    build: frontend/
    container_name: frontend
    ports:
      - 80:80
    networks:
      app:
networks:
  app: